# Testing dependencies for robocore-cli

# Core testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
pytest-timeout>=2.1.0

# Test utilities
paramiko>=3.0.0          # SSH connections for multi-host tests
pyyaml>=6.0              # YAML parsing for test configs
requests>=2.28.0         # HTTP requests for API testing
psutil>=5.9.0            # System monitoring in tests

# Docker testing
python-on-whales>=0.59.0  # Already in main requirements, but needed for tests

# Network and timing
netaddr>=0.8.0           # IP address handling
timeout-decorator>=0.5.0 # Test timeouts

# Mock and fixtures
responses>=0.23.0        # HTTP response mocking
freezegun>=1.2.0         # Time/date mocking
factory-boy>=3.2.0       # Test data factories

# Performance testing
memory-profiler>=0.60.0  # Memory usage profiling
py-spy>=0.3.0            # CPU profiling (optional)

# ROS testing utilities (optional, for ROS-specific tests)
# rclpy>=3.3.0           # Uncomment if testing with real ROS
# std-msgs>=4.2.0        # Uncomment if testing with real ROS

# Code quality (used in CI)
black>=22.0.0
flake8>=5.0.0
isort>=5.10.0

# Development utilities
ipython>=8.0.0           # Interactive debugging
ipdb>=0.13.0             # Debugging in tests